// There is no leave-balance/quota concept anywhere in the backend schema, so
// this stays a static display value only (kept as UI decoration per explicit
// product decision - not wired to any endpoint).
export const leaveBalance = {
  taken: 9,
  remaining: 26,
};
