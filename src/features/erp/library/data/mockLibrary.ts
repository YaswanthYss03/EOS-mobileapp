// There is no "library account info" endpoint anywhere in the backend, so
// this stays a static display value only (kept as UI decoration - not wired
// to any endpoint).
export const libraryInfo = {
  name: "Central Library",
  accountType: "Staff account",
};
