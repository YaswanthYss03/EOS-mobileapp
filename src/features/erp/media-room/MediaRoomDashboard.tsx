import { View, Text } from "react-native";

// TODO: media-room RBAC - view-only pages for this role go here as siblings/subfolders.
// Anything with complex operations (bulk edits, approvals, config) stays on the web app.
export function MediaRoomDashboard() {
  return (
    <View>
      <Text>MediaRoom Dashboard</Text>
    </View>
  );
}
